apiVersion: v1
kind: Service
metadata:
  name: {{ include "titan-aas.fullname" . }}
  labels:
    {{- include "titan-aas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "titan-aas.selectorLabels" . | nindent 4 }}
